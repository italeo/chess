package service;

/** Service responsible for user logout. */
public class LogoutService {

    /** Logs the user represented by the authToken out of the game.
     * @param authToken - The unique token provided to the user during log in.
     * */
    public boolean logout(String authToken) {
        return false;
    }
}
